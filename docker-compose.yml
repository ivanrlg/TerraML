services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - fuzzysat-data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ProjectStorage__BasePath=/app/data

volumes:
  fuzzysat-data:
